//using Microsoft.FluentUI.AspNetCore.Components;

namespace Rubik.Identity.Admin.Extensions
{
    internal static class TreeViewRecursionExtension
    {
        //public static List<TreeViewItem> ToTreeViewItems<T>(this IEnumerable<T> source,int? parentid=null)
        //    where T:BaseTreeEntity
        //{
        //    var result = new List<TreeViewItem>();

        //    foreach (var item in source.Where(a=>a.ParentID==parentid))
        //    {
        //        var treeItem = new TreeViewItem
        //        {
        //            Id = item.ID.ToString(),
        //            Text = item.Name!,
        //            Items = ToTreeViewItems(source, item.ID)
        //        };
        //        result.Add(treeItem);
        //    }

        //    return result;
        //}
    }
}
